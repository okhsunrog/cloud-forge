---
all:
  children:
    moscow:
      hosts:
        moscow:
          ansible_host: "okhsunrog.ru"
          ansible_user: ubuntu
          ansible_become: true

    europe:
      hosts:
        europe:
          ansible_host: "okhsunrog.dev"
          ansible_user: root
